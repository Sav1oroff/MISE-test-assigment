# Бронирование столика

Тестовое задание на Vue 3 / Nuxt 3 / TypeScript. Приложение имитирует бронирование столика в ресторане: пользователь
заполняет данные, выбирает доступную дату и часовой слот, после чего видит экран подтверждения.

## Запуск локально

Требования: Node.js 18+ и npm.

```bash
npm install
npm run dev
```

После запуска приложение доступно по адресу `http://localhost:3000`.

Проверка production-сборки:

```bash
npm run build
npm run preview
```

## Структура

```text
app/
  pages/index.vue                         # форма, loading и переключение экранов
  components/ConfirmationScreen/          # экран успешного бронирования
  composables/useBookingForm.ts           # состояние, слоты и правила валидации
  types/booking.ts                        # BookingFormData и BookingStatus
  assets/styles/main.css                  # общие токены и адаптивные стили
```

## Принятые решения

- Логику формы вынес в `useBookingForm`, чтобы страница отвечала за отображение, а вычисление дат, слотов и валидацию
  было сосредоточено в одном месте.
- Для валидации использован VeeValidate: ошибки доступны рядом с соответствующими полями и показываются при blur или
  попытке отправки.
- Состояние экрана описано типом `BookingStatus`: `idle`, `loading` и `success`; переходы между состояниями анимируются
  через встроенный Vue-компонент `<Transition>`.
- `ConfirmationScreen` получает данные бронирования одним типизированным prop `booking` и сообщает о сбросе формы через
  событие `reset`.
- Отправка симулируется задержкой `setTimeout` на 1,5 секунды, потому что по ТЗ backend и база данных не требуются.
- Доступные слоты строятся с шагом один час в диапазоне с 12:00 до 22:00; для текущей даты прошедшие слоты скрываются.

## Соответствие ТЗ

| Требование                                 | Статус       | Реализация                                                   |
|--------------------------------------------|--------------|--------------------------------------------------------------|
| Имя, телефон, дата, время, гости           | Готово       | Поля находятся в `index.vue`                                 |
| Все поля обязательны                       | Готово       | Схема VeeValidate                                            |
| Имя: от 2 символов, буквы, пробелы, дефисы | Готово       | `validateName`                                               |
| Телефон `+7` или `8`, 10 цифр              | Готово       | `validatePhone` нормализует ввод через удаление разделителей |
| Дата от доступной даты до `+90` дней       | Готово       | `min`/`max` у date input и `validateDate`                    |
| Слоты 12:00-22:00 с шагом 1 час            | Готово       | `availableTimeSlots`                                         |
| Ошибки при submit и blur                   | Готово       | VeeValidate и `errors` рядом с полями                        |
| Имитация отправки 1,5 секунды              | Готово       | `setTimeout` в `submit`                                      |
| Экран подтверждения с деталями             | Готово       | `ConfirmationScreen` получает `booking` через props          |
| Кнопка «Забронировать ещё»                 | Готово       | `resetBooking` возвращает форму                              |
| `<Transition>` для смены экранов           | Готово       | `mode="out-in"` и ключи `form`, `loading`, `success`         |
| Адаптивность 375px / 1280px                | Готово       | Вертикальная форма с ограниченной шириной и responsive CSS   |
| Backend / база данных                      | Не требуется | Отправка намеренно симулируется                              |

## Что бы я доделал при наличии времени

1. **Перенёс бы лоадер внутрь кнопки отправки и добавил `disabled` на время запроса, чтобы полностью совпасть с
   формулировкой ТЗ.
2. **Добавил бы иконку галочки на экран успешного бронирования.
3. **Уточнил бы проверку количества гостей через `Number.isInteger` и добавил `step="1"` для запрета дробных значений.
4. Вынес бы оставшуюся разметку формы из `index.vue` в `BookingForm.vue`, оставив composable источником общей логики.
5. Добавил бы unit-тесты для `useBookingForm` и проверил состояния формы на мобильном и desktop-размерах в браузере.

**не особо обязательно

## Ограничения

Данные не отправляются на сервер и не сохраняются после перезагрузки страницы. Доступность слотов рассчитывается только
на клиенте и не учитывает реальные занятые столики.
